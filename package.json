{
  "name": "peerhub-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && node index.js",
    "build": "cd server && npm install && npx prisma generate"
  },
  "dependencies": {
    "streamifier": "^0.1.1"
  }
}
